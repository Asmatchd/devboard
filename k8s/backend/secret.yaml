# Secrets are base64 encoded — never store plain text passwords in K8s
# Generate with: echo -n "value" | base64
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
type: Opaque
data:
  # echo -n "password" | base64
  POSTGRES_PASSWORD: cGFzc3dvcmQ=
  # echo -n "devboard-local-secret-key-2024" | base64
  JWT_SECRET: ZGV2Ym9hcmQtbG9jYWwtc2VjcmV0LWtleS0yMDI0
